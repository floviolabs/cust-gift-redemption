const HeaderPrimaryBig = ({ text}: {text:string}) => {
    return(
        <>
            <h1 className={`text-xl font-bold text-primary`}>{text}</h1>
        </>
    )
}

export default HeaderPrimaryBig